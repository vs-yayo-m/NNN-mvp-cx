# Groq API key — required for AI-powered search and recommendations.
# Get a free key at https://console.groq.com/keys (free tier, no credit
# card required). Without this set, the app still works fully — AI search
# and recommendations automatically fall back to local logic.
GROQ_API_KEY=
