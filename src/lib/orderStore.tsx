'use client';
import React,{createContext,useContext,useEffect,useState}from'react';import{CheckoutDetails,Order,CartLine,RecurringOrder}from'@/types';import{readStore,writeStore}from'./storage';import{uid}from'./utils';
const C=createContext<any>(null);export function OrderProvider({children}:{children:React.ReactNode}){const[orders,setOrders]=useState<Order[]>([]);const[recurring,setRecurring]=useState<RecurringOrder[]>([]);useEffect(()=>{setOrders(readStore('nnn-orders',[]));setRecurring(readStore('nnn-recurring',[]))},[]);useEffect(()=>writeStore('nnn-orders',orders),[orders]);useEffect(()=>writeStore('nnn-recurring',recurring),[recurring]);const createOrder=(lines:CartLine[],details:CheckoutDetails,subtotal:number,deliveryFee:number,discount:number,total:number)=>{const o={id:uid('NNN'),createdAt:new Date().toISOString(),lines,details,subtotal,deliveryFee,discount,total};setOrders(x=>[o,...x]);writeStore('nnn-last-order',o.id);return o};const reorder=(o:Order)=>o.lines;const addRecurring=(r:Omit<RecurringOrder,'id'|'paused'>)=>setRecurring(x=>[{...r,id:uid('rec'),paused:false},...x]);return <C.Provider value={{orders,recurring,createOrder,reorder,addRecurring,setRecurring}}>{children}</C.Provider>}
export const useOrders=()=>useContext(C);
