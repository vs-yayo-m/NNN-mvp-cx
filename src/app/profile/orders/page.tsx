'use client';import Link from'next/link';import{useOrders}from'@/lib/orderStore';import{formatCurrency}from'@/lib/utils';export default function Orders(){const o=useOrders();return <div><h1 className="mb-4 font-display text-4xl font-black">Order History</h1>{o.orders.length?o.orders.map((x:any)=><Link href={`/order/${x.id}`} className="panel mb-3 block p-5" key={x.id}><b>{x.id}</b><p>{x.lines.length} items · {formatCurrency(x.total)}</p></Link>):<div className="panel p-8">No orders yet. Your completed Nom Nom orders will appear here.</div>}</div>}
