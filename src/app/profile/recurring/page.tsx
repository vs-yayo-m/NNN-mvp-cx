'use client';import{useOrders}from'@/lib/orderStore';export default function Recurring(){const o=useOrders();return <div><h1 className="mb-4 font-display text-4xl font-black">Recurring Orders</h1>{o.recurring.length?o.recurring.map((r:any)=><div className="panel mb-3 flex items-center justify-between p-5" key={r.id}><p><b>{r.summary}</b><br/>{r.frequency} at {r.time}</p><button className="rounded-full bg-orange-50 px-4 py-2" onClick={()=>o.setRecurring(o.recurring.filter((x:any)=>x.id!==r.id))}>Delete</button></div>):<div className="panel p-8">No recurring orders yet. Create one from a cart or past order.</div>}</div>}
