'use client';
import{useSearchParams}from'next/navigation';import{useState}from'react';import{availableMenu}from'@/data/menu';import{CategoryFilterBar,MenuGrid}from'@/modules/menu';
export default function MenuPage(){const sp=useSearchParams();const[cat,setCat]=useState(sp.get('category')||'all');const[veg,setVeg]=useState('all');const[sort,setSort]=useState('popular');let items=availableMenu().filter(i=>(cat==='all'||i.categoryId===cat)&&(veg==='all'||(veg==='veg'?i.isVeg:!i.isVeg)));items=[...items].sort((a,b)=>sort==='low'?a.variants[0].price-b.variants[0].price:sort==='high'?b.variants[0].price-a.variants[0].price:Number(b.isPopular)-Number(a.isPopular));return <><h1 className="mb-4 font-display text-4xl font-black">Browse the Butwal menu</h1><CategoryFilterBar category={cat} setCategory={setCat} veg={veg} setVeg={setVeg} sort={sort} setSort={setSort}/><MenuGrid items={items}/></>}
