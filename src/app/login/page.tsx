'use client';import{useRouter,useSearchParams}from'next/navigation';import{useAuth}from'@/lib/authStore';import PhoneInputForm from'@/modules/auth/components/PhoneInputForm';import{writeStore}from'@/lib/storage';export default function Login(){const r=useRouter(),sp=useSearchParams(),auth=useAuth();return <PhoneInputForm onSubmit={p=>{auth.startLogin(p);writeStore('nnn-return',sp.get('return')||'/profile');r.push('/login/otp')}}/>}
