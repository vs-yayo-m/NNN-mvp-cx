'use client';import{useState}from'react';export default function RecurringOrderForm({onSave,summary,lines}:any){const[f,setF]=useState('Weekdays'),[time,setTime]=useState('13:00'),[start,setStart]=useState(new Date().toISOString().slice(0,10));return <div className="panel p-5"><h2 className="font-display text-2xl font-black">Make this recurring</h2><select className="input my-2" value={f} onChange={e=>setF(e.target.value)}>{['Daily','Weekdays','Weekly'].map(x=><option key={x}>{x}</option>)}</select><input className="input my-2" type="time" value={time} onChange={e=>setTime(e.target.value)}/><input className="input my-2" type="date" value={start} onChange={e=>setStart(e.target.value)}/><button className="btn" onClick={()=>onSave({summary,lines,frequency:f,time,startDate:start})}>Save recurring order</button></div>}
