'use client';
import{MenuItem}from'@/types';import{formatCurrency}from'@/lib/utils';import MenuImage from'./MenuImage';
export default function MenuItemCard({item,onSelect}:{item:MenuItem;onSelect:(i:MenuItem)=>void}){return <button onClick={()=>onSelect(item)} className="panel overflow-hidden text-left transition hover:-translate-y-1"><div className="relative h-36"><MenuImage image={item.image} alt={item.name}/></div><div className="p-4"><p className="font-display text-xl font-bold">{item.name}</p><p className="line-clamp-2 min-h-10 text-sm text-stone-600">{item.description||item.tags.join(' • ')}</p><div className="mt-3 flex items-center justify-between"><span className="font-bold text-brand">{formatCurrency(item.variants[0].price)}+</span><span>{item.isVeg?'🟢':'🔴'}</span></div></div></button>}
