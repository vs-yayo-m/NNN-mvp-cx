'use client';
import{categories}from'@/data/categories';
export default function CategoryFilterBar({category,setCategory,veg,setVeg,sort,setSort}:{category:string;setCategory:(s:string)=>void;veg:string;setVeg:(s:string)=>void;sort:string;setSort:(s:string)=>void}){return <div className="mb-6 space-y-3"><div className="flex gap-2 overflow-x-auto pb-2"><button onClick={()=>setCategory('all')} className="btn bg-ink">All</button>{categories.map(c=><button className="rounded-full bg-white px-4 py-2 font-bold" key={c.id} onClick={()=>setCategory(c.id)}>{c.icon} {c.label}</button>)}</div><div className="flex gap-2"><select className="input" value={veg} onChange={e=>setVeg(e.target.value)}><option value="all">All diets</option><option value="veg">Veg only</option><option value="nonveg">Non-veg</option></select><select className="input" value={sort} onChange={e=>setSort(e.target.value)}><option value="popular">Popular first</option><option value="low">Price low to high</option><option value="high">Price high to low</option></select></div></div>}
