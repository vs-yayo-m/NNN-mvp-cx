'use client';import{useEffect,useState}from'react';export const ORDER_TIMELINE=[['Order Received',0],['Preparing',10000],['Out for Delivery / Ready for Pickup',25000],['Delivered / Completed',45000]] as const;export default function OrderStatusStepper({createdAt}:{createdAt:string}){const[now,setNow]=useState(Date.now());useEffect(()=>{const t=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(t)},[]);const elapsed=now-new Date(createdAt).getTime();const active=ORDER_TIMELINE.filter(([,ms])=>elapsed>=ms).length-1;return <ol className="panel p-5">{ORDER_TIMELINE.map(([label],i)=><li key={label} className="flex gap-3 py-3"><span className={`grid h-8 w-8 place-items-center rounded-full ${i<=active?'bg-brand text-white':'bg-orange-100'}`}>{i+1}</span><b>{label}</b></li>)}</ol>}
