'use client';import Link from'next/link';import{useCart}from'@/lib/cartStore';import{formatCurrency}from'@/lib/utils';import CartLineItem from'./CartLineItem';import PromoCodeInput from'./PromoCodeInput';export default function CartDrawer(){const c=useCart();return <div className="panel p-5">{c.lines.length?c.lines.map((l:any)=><CartLineItem key={l.lineId} line={l}/>):<p>Your cart is empty. Add momo, pizza or bar favourites to begin.</p>}<div className="my-4"><PromoCodeInput/></div><div className="space-y-1 font-bold"><p>Subtotal {formatCurrency(c.subtotal)}</p><p>Delivery {formatCurrency(c.deliveryFee)}</p><p>Discount -{formatCurrency(c.discount)}</p><p className="text-xl text-brand">Total {formatCurrency(c.total)}</p></div>{c.lines.length>0&&<Link className="btn mt-4 inline-block" href="/checkout">Proceed to Checkout</Link>}</div>}
