'use client';import{CartLine}from'@/types';import{formatCurrency}from'@/lib/utils';import{useCart}from'@/lib/cartStore';export default function CartLineItem({line}:{line:CartLine}){const c=useCart();return <div className="flex items-center justify-between border-b py-3"><div><b>{line.name}</b><p className="text-sm text-stone-500">{line.variant.label} · {formatCurrency(line.variant.price)}</p></div><div className="flex items-center gap-2"><button onClick={()=>c.setQty(line.lineId,line.quantity-1)}>-</button><b>{line.quantity}</b><button onClick={()=>c.setQty(line.lineId,line.quantity+1)}>+</button></div></div>}
