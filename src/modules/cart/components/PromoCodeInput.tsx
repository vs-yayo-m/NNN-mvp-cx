'use client';import{useState}from'react';import{useCart}from'@/lib/cartStore';export default function PromoCodeInput(){const[c,setC]=useState('');const[msg,setMsg]=useState('');const cart=useCart();return <div className="flex gap-2"><input className="input" placeholder="WELCOME10" value={c} onChange={e=>setC(e.target.value)}/><button className="btn" onClick={()=>setMsg(cart.applyPromo(c)?'Applied':'Try WELCOME10 or NOMNOM50')}>Apply</button><span className="text-sm">{msg}</span></div>}
