'use client';import{useState}from'react';import SearchResultsList from'@/modules/search/components/SearchResultsList';export default function AskAIPanel(){const[q,setQ]=useState('spicy and quick');const[ask,setAsk]=useState('');return <section className="panel p-5"><h2 className="font-display text-3xl font-black">Not sure what to eat?</h2><div className="mt-3 flex gap-2"><input className="input" value={q} onChange={e=>setQ(e.target.value)}/><button className="btn" onClick={()=>setAsk(q)}>Ask AI</button></div>{ask&&<div className="mt-4"><SearchResultsList query={ask}/></div>}</section>}
