'use client';import{useCart}from'@/lib/cartStore';import{formatCurrency}from'@/lib/utils';export default function OrderReviewSummary(){const c=useCart();return <div className="panel p-5"><h2 className="font-display text-2xl font-black">Order Review</h2>{c.lines.map((l:any)=><p key={l.lineId}>{l.quantity}× {l.name} ({l.variant.label})</p>)}<b className="text-brand">Total {formatCurrency(c.total)}</b></div>}
