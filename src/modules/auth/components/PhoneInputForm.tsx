'use client';import{useState}from'react';export default function PhoneInputForm({onSubmit}:{onSubmit:(p:string)=>void}){const[p,setP]=useState('');const ok=/^\d{10}$/.test(p);return <div className="panel mx-auto max-w-md p-6"><h1 className="font-display text-4xl font-black">Login</h1><label className="mt-4 flex items-center gap-2 rounded-2xl bg-white px-3"><b>+977</b><input className="input border-0" value={p} onChange={e=>setP(e.target.value.replace(/\D/g,'').slice(0,10))} placeholder="98XXXXXXXX"/></label><button className="btn mt-5" disabled={!ok} onClick={()=>onSubmit(p)}>Continue</button>{!ok&&p&&<p className="text-sm text-brand">Use a 10 digit Nepal phone number.</p>}</div>}
